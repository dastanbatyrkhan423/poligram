# Инструкция по отладке ошибки 500 на Render

## 🔍 Как найти причину ошибки 500

### Шаг 1: Включите DEBUG режим (временно)

1. В Render Dashboard откройте ваш Web Service
2. Перейдите в **Environment** → **Environment Variables**
3. Найдите переменную `DEBUG`
4. Измените значение на `True`
5. Сохраните и подождите перезапуска (1-2 минуты)

Теперь на сайте будет видна полная ошибка вместо "Server Error (500)".

### Шаг 2: Проверьте логи

1. В Render Dashboard откройте ваш Web Service
2. Перейдите в раздел **Logs**
3. Прокрутите вниз до последних ошибок
4. Ищите строки с `ERROR`, `Exception`, `Traceback`

### Шаг 3: Проверьте переменные окружения

Убедитесь, что установлены ВСЕ переменные:

```
SECRET_KEY=ваш-секретный-ключ
DEBUG=False (или True для отладки)
ALLOWED_HOSTS=poligram-website.onrender.com,*.onrender.com
DATABASE_URL=автоматически из базы данных
```

## ✅ Обязательные переменные для Render

### 1. SECRET_KEY
```bash
# Сгенерируйте новый ключ:
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

### 2. DEBUG
- Для продакшена: `False`
- Для отладки: `True` (временно!)

### 3. ALLOWED_HOSTS
```
poligram-website.onrender.com,*.onrender.com
```
Или ваш конкретный URL, который дал Render.

### 4. DATABASE_URL
Автоматически подставляется из настроек базы данных в `render.yaml`.

## 🛠️ Частые проблемы и решения

### Проблема 1: "ModuleNotFoundError"
**Решение:** Проверьте `requirements.txt` - все зависимости должны быть указаны.

### Проблема 2: "Database connection failed"
**Решение:** 
- Убедитесь, что база данных создана
- Проверьте, что `DATABASE_URL` правильно настроен
- В Render база данных должна быть в том же регионе, что и Web Service

### Проблема 3: "Static files not found"
**Решение:**
- Убедитесь, что `build.sh` содержит `python manage.py collectstatic --no-input`
- Проверьте, что WhiteNoise включен (он автоматически включается при `DEBUG=False`)

### Проблема 4: "ALLOWED_HOSTS" ошибка
**Решение:**
- Добавьте ваш Render URL в `ALLOWED_HOSTS`
- Формат: `poligram-website.onrender.com` или `*.onrender.com`

## 📋 Чеклист перед деплоем

- [ ] Все зависимости в `requirements.txt`
- [ ] `build.sh` существует и исполняемый
- [ ] `render.yaml` настроен правильно
- [ ] `SECRET_KEY` установлен в Environment Variables
- [ ] `DEBUG=False` для продакшена
- [ ] `ALLOWED_HOSTS` содержит ваш Render URL
- [ ] База данных создана и подключена
- [ ] Миграции применены (автоматически в `build.sh`)

## 🔧 Команды для проверки локально

```bash
# Проверка настроек
python manage.py check --deploy

# Проверка статических файлов
python manage.py collectstatic --dry-run

# Проверка миграций
python manage.py showmigrations
```

## 📞 Если ничего не помогает

1. Включите `DEBUG=True` в Render
2. Откройте сайт и скопируйте полный traceback ошибки
3. Проверьте логи в Render Dashboard
4. Убедитесь, что все переменные окружения установлены

