# 📋 Резюме изменений для деплоя на Render

## ✅ Что было изменено в коде:

### 1. `poligram_website/settings.py`

**Изменения:**
- ✅ `DEBUG` по умолчанию `False` (для продакшена)
- ✅ `ALLOWED_HOSTS` автоматически настраивается для продакшена
- ✅ Добавлены настройки безопасности для продакшена:
  - `SECURE_BROWSER_XSS_FILTER = True`
  - `SECURE_CONTENT_TYPE_NOSNIFF = True`
  - `X_FRAME_OPTIONS = 'DENY'`

### 2. `requirements.txt`

**Добавлено:**
- ✅ `django-ckeditor[uploader]==6.7.0` (для загрузки файлов в CKEditor)

### 3. `render.yaml`

**Изменения:**
- ✅ Start Command обновлен: добавлен `--bind 0.0.0.0:$PORT`
- ✅ Добавлен `ALLOWED_HOSTS` в переменные окружения

### 4. `build.sh`

**Улучшения:**
- ✅ Добавлено обновление pip
- ✅ Улучшена обработка ошибок

### 5. `Procfile` (новый файл)

**Создан для альтернативного способа деплоя:**
```
web: gunicorn poligram_website.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120
```

---

## 🚀 ФИНАЛЬНАЯ START COMMAND для Render:

```bash
gunicorn poligram_website.wsgi:application --bind 0.0.0.0:$PORT
```

**Или с дополнительными параметрами (рекомендуется):**

```bash
gunicorn poligram_website.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120
```

---

## 📝 ПЕРЕМЕННЫЕ ОКРУЖЕНИЯ для Render:

### Обязательные переменные:

| Переменная | Значение | Как получить |
|-----------|----------|--------------|
| `SECRET_KEY` | `сгенерированный-ключ` | См. команду ниже |
| `DEBUG` | `False` | Для продакшена |
| `ALLOWED_HOSTS` | `ваш-сайт.onrender.com,*.onrender.com` | После деплоя |
| `DATABASE_URL` | `postgresql://...` | Из настроек БД на Render |

### Команда для генерации SECRET_KEY:

```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

---

## 🔧 BUILD COMMAND:

```bash
./build.sh
```

---

## ⚠️ ВАЖНО: Причины ошибки 500 и решения

### 1. Не установлены переменные окружения
**Симптом:** `Server Error (500)` сразу после деплоя
**Решение:** Убедитесь, что установлены ВСЕ переменные из таблицы выше

### 2. DEBUG=False скрывает ошибку
**Симптом:** Видите только "Server Error (500)", без деталей
**Решение:** 
- Временно установите `DEBUG=True` в Environment Variables
- Перезапустите сервис
- Откройте сайт - увидите полную ошибку
- Исправьте проблему
- Верните `DEBUG=False`

### 3. Неправильный ALLOWED_HOSTS
**Симптом:** Ошибка `DisallowedHost`
**Решение:** Добавьте ваш Render URL в `ALLOWED_HOSTS`

### 4. База данных не подключена
**Симптом:** `Database connection failed`
**Решение:**
- Убедитесь, что база данных создана
- Проверьте `DATABASE_URL` в Environment Variables
- База данных должна быть в том же регионе, что и Web Service

### 5. Статические файлы не собраны
**Симптом:** CSS/JS не загружаются
**Решение:**
- Убедитесь, что `build.sh` содержит `collectstatic`
- Проверьте, что WhiteNoise включен (автоматически при `DEBUG=False`)

### 6. Неправильная команда запуска
**Симптом:** Сервис не запускается
**Решение:** Используйте правильную команду:
```bash
gunicorn poligram_website.wsgi:application --bind 0.0.0.0:$PORT
```

---

## 📋 Чеклист перед деплоем:

- [x] `requirements.txt` обновлен
- [x] `build.sh` готов
- [x] `render.yaml` настроен
- [x] `settings.py` настроен для продакшена
- [x] Start Command правильный
- [ ] Код загружен на GitHub
- [ ] База данных создана на Render
- [ ] Все переменные окружения установлены

---

## 🎯 Следующие шаги:

1. Загрузите код на GitHub
2. Создайте базу данных PostgreSQL на Render
3. Создайте Web Service на Render
4. Установите переменные окружения
5. Задеплойте!

Подробная инструкция в файле: `RENDER_DEPLOY_FINAL.md`


