# 🚀 Финальная инструкция по деплою на Render

## ✅ Что уже готово в проекте:

1. ✅ **requirements.txt** - все зависимости указаны
2. ✅ **build.sh** - скрипт сборки готов
3. ✅ **render.yaml** - конфигурация для автоматического деплоя
4. ✅ **settings.py** - настроен для продакшена
5. ✅ **База данных** - настроена для PostgreSQL на Render
6. ✅ **WhiteNoise** - для статических файлов
7. ✅ **Gunicorn** - для запуска приложения

---

## 📋 Пошаговая инструкция деплоя

### Шаг 1: Загрузите код на GitHub

```bash
git add .
git commit -m "Prepare for Render deployment"
git push origin main
```

### Шаг 2: Создайте базу данных PostgreSQL на Render

1. В Render Dashboard нажмите **"New +"** → **"PostgreSQL"**
2. Настройки:
   - **Name**: `poligram-db`
   - **Database**: `poligram`
   - **User**: `poligram_user`
   - **Region**: `Frankfurt` (или ближайший к вам)
   - **Plan**: `Starter` (бесплатный)
3. Нажмите **"Create Database"**
4. После создания скопируйте **Internal Database URL** (будет нужен позже)

### Шаг 3: Создайте Web Service

1. В Render Dashboard нажмите **"New +"** → **"Web Service"**
2. Подключите ваш GitHub репозиторий: `citatydusi-sys/Poligram`
3. Настройки сервиса:

#### Основные настройки:
- **Name**: `poligram-website`
- **Region**: тот же, что и база данных
- **Branch**: `main`
- **Root Directory**: оставьте пустым
- **Environment**: `Python 3`
- **Build Command**: `./build.sh`
- **Start Command**: `gunicorn poligram_website.wsgi:application --bind 0.0.0.0:$PORT`

#### Python Version:
- **Python Version**: `3.11.0` (или `3.11`)

### Шаг 4: Настройте переменные окружения

В разделе **"Environment"** добавьте следующие переменные:

#### Обязательные переменные:

1. **SECRET_KEY**
   ```
   Значение: сгенерируйте новый ключ (см. ниже)
   ```
   
   **Как сгенерировать:**
   ```bash
   python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
   ```

2. **DEBUG**
   ```
   Значение: False
   ```
   ⚠️ Для отладки можно временно поставить `True`, но потом обязательно верните `False`!

3. **ALLOWED_HOSTS**
   ```
   Значение: poligram-website.onrender.com,*.onrender.com
   ```
   ⚠️ После деплоя Render даст вам URL, замените `poligram-website` на ваш реальный URL!

4. **DATABASE_URL**
   ```
   Значение: скопируйте Internal Database URL из настроек базы данных
   ```
   Формат: `postgresql://user:password@host:port/database`

#### Опциональные переменные (если нужны):

- `PYTHON_VERSION`: `3.11.0` (если не указано в настройках)

### Шаг 5: Задеплойте

1. Нажмите **"Create Web Service"**
2. Render начнет деплой (займет 5-10 минут)
3. Следите за логами в разделе **"Logs"**

---

## 🔧 Финальная Start Command для Render

```bash
gunicorn poligram_website.wsgi:application --bind 0.0.0.0:$PORT
```

Или с дополнительными параметрами (рекомендуется):

```bash
gunicorn poligram_website.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120
```

---

## 📝 Переменные окружения для Render

### Минимальный набор (обязательно):

| Переменная | Значение | Описание |
|-----------|----------|----------|
| `SECRET_KEY` | `сгенерированный-ключ` | Секретный ключ Django |
| `DEBUG` | `False` | Режим отладки (False для продакшена) |
| `ALLOWED_HOSTS` | `ваш-сайт.onrender.com,*.onrender.com` | Разрешенные хосты |
| `DATABASE_URL` | `postgresql://...` | URL базы данных (из настроек БД) |

### Как получить значения:

**SECRET_KEY:**
```bash
python -c "from django.core.management.utils import get_random_secret_key; print(get_random_secret_key())"
```

**DATABASE_URL:**
- Render Dashboard → Ваша база данных → **Internal Database URL**

**ALLOWED_HOSTS:**
- После деплоя Render даст вам URL вида: `poligram-website-xxxx.onrender.com`
- Используйте этот URL в `ALLOWED_HOSTS`

---

## 🐛 Решение ошибки 500

### Если видите "Server Error (500)":

#### 1. Включите DEBUG режим (временно):

В Render Dashboard → Environment Variables:
- Измените `DEBUG` на `True`
- Сохраните и подождите перезапуска (1-2 минуты)
- Откройте сайт - увидите полную ошибку

#### 2. Проверьте логи:

Render Dashboard → **Logs** → прокрутите вниз до последних ошибок

Ищите:
- `ERROR`
- `Exception`
- `Traceback`
- `ModuleNotFoundError`
- `Database connection failed`

#### 3. Частые причины и решения:

**Проблема: ModuleNotFoundError**
```
Решение: Проверьте requirements.txt - все зависимости должны быть указаны
```

**Проблема: Database connection failed**
```
Решение: 
- Убедитесь, что DATABASE_URL правильный
- База данных должна быть в том же регионе, что и Web Service
- Проверьте, что база данных создана и активна
```

**Проблема: ALLOWED_HOSTS error**
```
Решение:
- Добавьте ваш Render URL в ALLOWED_HOSTS
- Формат: ваш-сайт.onrender.com,*.onrender.com
```

**Проблема: Static files not found**
```
Решение:
- Убедитесь, что build.sh содержит collectstatic
- Проверьте, что WhiteNoise включен (автоматически при DEBUG=False)
```

**Проблема: SECRET_KEY missing**
```
Решение:
- Убедитесь, что SECRET_KEY установлен в Environment Variables
- Сгенерируйте новый ключ и добавьте его
```

#### 4. После исправления:

- Верните `DEBUG=False`
- Перезапустите сервис (Manual Deploy)

---

## ✅ Чеклист перед деплоем

- [ ] Код загружен на GitHub
- [ ] `requirements.txt` содержит все зависимости
- [ ] `build.sh` существует и исполняемый
- [ ] `render.yaml` настроен (опционально)
- [ ] База данных PostgreSQL создана на Render
- [ ] Web Service создан
- [ ] `SECRET_KEY` установлен в Environment Variables
- [ ] `DEBUG=False` установлен
- [ ] `ALLOWED_HOSTS` содержит ваш Render URL
- [ ] `DATABASE_URL` установлен (из настроек БД)
- [ ] Start Command: `gunicorn poligram_website.wsgi:application --bind 0.0.0.0:$PORT`

---

## 🔍 Проверка после деплоя

1. **Откройте сайт** - должен загрузиться без ошибок
2. **Проверьте админку**: `https://ваш-сайт.onrender.com/admin/`
3. **Создайте суперпользователя**:
   - Render Dashboard → Web Service → **Shell**
   - Выполните:
   ```bash
   python manage.py createsuperuser
   ```

---

## 📞 Если ничего не помогает

1. Включите `DEBUG=True` временно
2. Откройте сайт и скопируйте полный traceback
3. Проверьте логи в Render Dashboard
4. Убедитесь, что все переменные окружения установлены
5. Проверьте, что база данных активна и доступна

---

## 🎯 Итоговые команды для Render

**Build Command:**
```bash
./build.sh
```

**Start Command:**
```bash
gunicorn poligram_website.wsgi:application --bind 0.0.0.0:$PORT
```

**Или с параметрами:**
```bash
gunicorn poligram_website.wsgi:application --bind 0.0.0.0:$PORT --workers 2 --timeout 120
```

---

Проект готов к деплою! 🚀

